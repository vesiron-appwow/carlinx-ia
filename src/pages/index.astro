---
import BaseLayout from '../layouts/BaseLayout.astro';
import SearchPanel from '../components/SearchPanel.astro';
import VehicleCard from '../components/VehicleCard.astro';
import { getVehicles } from '../lib/api';
import { VEHICLE_TYPES } from '../lib/makes';

const url    = new URL(Astro.request.url);
const params: Record<string, string> = {};
url.searchParams.forEach((v, k) => { if (v) params[k] = v; });

const activeType = params.type ?? 'car';
const activeTypeInfo = VEHICLE_TYPES.find(t => t.value === activeType) ?? VEHICLE_TYPES[0];

const searchParams = {
  type:         activeType,
  make:         params.make_custom || params.make || undefined,
  model:        params.model        || undefined,
  fuel:         params.fuel         || undefined,
  transmission: params.transmission || undefined,
  body_type:    params.body_type    || undefined,
  min_price:    params.min_price    ? Number(params.min_price)   : undefined,
  max_price:    params.max_price    ? Number(params.max_price)   : undefined,
  max_mileage:  params.max_mileage  ? Number(params.max_mileage) : undefined,
  year_from:    params.year_from    ? Number(params.year_from)   : undefined,
  location:     params.location     || undefined,
  sort:         params.sort         || 'created_at_desc',
  page:         params.page         ? Number(params.page) : 1,
  limit:        24,
};

const { vehicles, total } = await getVehicles(searchParams, fetch);

const hasSearch = !!(params.make || params.model || params.fuel || params.min_price ||
  params.max_price || params.max_mileage || params.year_from || params.location || params.body_type);

const totalPages  = Math.ceil(total / 24);
const currentPage = searchParams.page ?? 1;

function buildPageUrl(page: number) {
  const p = new URLSearchParams(url.searchParams);
  p.set('page', String(page));
  return `/?${p.toString()}`;
}
---

<BaseLayout
  title={`CarLinx — ${activeTypeInfo.label} Discovery`}
  description={`Search ${activeTypeInfo.label.toLowerCase()} for sale worldwide. Filter by make, model, price, mileage and more.`}
>

  <!-- HERO -->
  <section class="hero">
    <div class="hero-inner">
      <p class="hero-eyebrow">Global {activeTypeInfo.label} Discovery</p>
      <h1 class="hero-title">Find Your Next<br /><em>Great {activeTypeInfo.label === 'Cars' ? 'Drive' : activeTypeInfo.label === 'Motorbikes' ? 'Ride' : 'Vehicle'}</em></h1>
      <p class="hero-sub">Search vehicles for sale worldwide — every make, every price point.</p>
    </div>
  </section>

  <!-- SEARCH PANEL (type tabs + filters) -->
  <SearchPanel params={params} />

  <!-- RESULTS BAR -->
  <div class="results-bar">
    <div class="results-inner">
      <p class="results-count" aria-live="polite">
        {total > 0
          ? <><strong>{total.toLocaleString('en-GB')}</strong> {activeTypeInfo.label.toLowerCase()} found</>
          : hasSearch
            ? `No ${activeTypeInfo.label.toLowerCase()} matched your search`
            : `Select filters to search ${activeTypeInfo.label.toLowerCase()}`
        }
      </p>
      <div class="sort-group">
        <label for="sort-sel">Sort</label>
        <form method="get" id="sort-form">
          {Object.entries(params).filter(([k]) => k !== 'sort' && k !== 'page').map(([k, v]) => (
            <input type="hidden" name={k} value={v} />
          ))}
          <select id="sort-sel" name="sort" onchange="this.form.submit()">
            <option value="created_at_desc" selected={!params.sort || params.sort === 'created_at_desc'}>Newest Listed</option>
            <option value="price_asc"       selected={params.sort === 'price_asc'}>Price: Low–High</option>
            <option value="price_desc"      selected={params.sort === 'price_desc'}>Price: High–Low</option>
            <option value="mileage_asc"     selected={params.sort === 'mileage_asc'}>Mileage: Low–High</option>
            <option value="year_desc"       selected={params.sort === 'year_desc'}>Year: Newest</option>
          </select>
        </form>
      </div>
    </div>
  </div>

  <!-- GRID / EMPTY STATES -->
  {vehicles.length > 0 ? (
    <section class="grid-section" aria-label={`${activeTypeInfo.label} results`}>
      <div class="vehicle-grid">
        {vehicles.map((v, i) => (
          <div class="fade-up" style={`animation-delay:${Math.min(i * 0.05, 0.4)}s`}>
            <VehicleCard vehicle={v} />
          </div>
        ))}
      </div>
      {totalPages > 1 && (
        <nav class="pagination" aria-label="Page navigation">
          {currentPage > 1   && <a href={buildPageUrl(currentPage - 1)} class="page-btn">← Prev</a>}
          <span class="page-info">Page {currentPage} of {totalPages}</span>
          {currentPage < totalPages && <a href={buildPageUrl(currentPage + 1)} class="page-btn">Next →</a>}
        </nav>
      )}
    </section>
  ) : hasSearch ? (
    <div class="empty-state">
      <div class="empty-icon">{activeTypeInfo.icon}</div>
      <h2>No {activeTypeInfo.label.toLowerCase()} found</h2>
      <p>Try adjusting your filters — remove a few criteria to broaden your results.</p>
      <a href={`/?type=${activeType}`} class="btn-clear">Clear filters</a>
    </div>
  ) : (
    <div class="empty-state">
      <div class="empty-icon">{activeTypeInfo.icon}</div>
      <h2>Search {activeTypeInfo.label}</h2>
      <p>Use the filters above to start your search — every field is optional.</p>
    </div>
  )}

</BaseLayout>

<script>
  function toggleSave(btn: HTMLButtonElement) {
    const id = btn.dataset.vehicleId;
    if (!id) return;
    const saved: string[] = JSON.parse(localStorage.getItem('cl_saved') ?? '[]');
    const idx = saved.indexOf(id);
    if (idx > -1) { saved.splice(idx, 1); btn.classList.remove('saved'); }
    else          { saved.push(id);        btn.classList.add('saved'); }
    localStorage.setItem('cl_saved', JSON.stringify(saved));
  }
  document.addEventListener('DOMContentLoaded', () => {
    const saved: string[] = JSON.parse(localStorage.getItem('cl_saved') ?? '[]');
    saved.forEach(id => {
      document.querySelector<HTMLButtonElement>(`.save-btn[data-vehicle-id="${id}"]`)?.classList.add('saved');
    });
  });
  (window as any).toggleSave = toggleSave;
</script>

<style>
  .hero {
    background: linear-gradient(150deg, #0C0C0C 0%, #160D00 60%, #0C0C0C 100%);
    border-bottom: 1px solid var(--border);
    padding: 3rem 1.5rem 2.5rem; position: relative; overflow: hidden;
  }
  .hero::before {
    content: ''; position: absolute; inset: 0;
    background: radial-gradient(ellipse 70% 90% at 75% 50%, rgba(255,168,0,0.07), transparent 70%);
    pointer-events: none;
  }
  .hero-inner { max-width: 1320px; margin: 0 auto; position: relative; }
  .hero-eyebrow {
    font-family: var(--font-head); font-weight: 700;
    font-size: 0.75rem; letter-spacing: 0.2em; text-transform: uppercase;
    color: var(--amber); margin-bottom: 0.625rem;
  }
  .hero-title {
    font-family: var(--font-head); font-weight: 900;
    font-size: clamp(2.2rem, 5.5vw, 3.8rem);
    color: white; line-height: 1; margin-bottom: 0.5rem;
  }
  .hero-title em { color: var(--amber); font-style: normal; }
  .hero-sub { color: var(--mute); font-size: 1rem; }

  .results-bar { border-bottom: 1px solid var(--border); padding: 0.875rem 1.5rem; }
  .results-inner {
    max-width: 1320px; margin: 0 auto;
    display: flex; justify-content: space-between; align-items: center;
    flex-wrap: wrap; gap: 0.75rem;
  }
  .results-count { font-size: 0.95rem; color: var(--mute); }
  .results-count strong { color: var(--amber); font-size: 1.1rem; }

  .sort-group { display: flex; align-items: center; gap: 0.5rem; }
  .sort-group label { font-size: 0.8rem; color: var(--mute); font-weight: 600; }
  .sort-group select {
    background: var(--panel); border: 1px solid var(--border);
    border-radius: 7px; padding: 0.4rem 0.75rem;
    color: var(--text); font-family: var(--font-body); font-size: 0.875rem; outline: none;
  }

  .grid-section { max-width: 1320px; margin: 0 auto; padding: 2rem 1.5rem 4rem; }
  .vehicle-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
    gap: 1.25rem; margin-bottom: 3rem;
  }

  .pagination {
    display: flex; align-items: center; justify-content: center; gap: 1.25rem;
    padding-top: 1rem; border-top: 1px solid var(--border);
  }
  .page-btn {
    padding: 0.6rem 1.25rem; border-radius: 7px;
    border: 1px solid var(--border); background: var(--panel);
    color: var(--text); text-decoration: none; font-weight: 600; transition: all 0.2s;
  }
  .page-btn:hover { border-color: var(--amber); color: var(--amber); }
  .page-info { color: var(--mute); font-size: 0.9rem; }

  .empty-state {
    max-width: 440px; margin: 5rem auto; padding: 0 1.5rem; text-align: center;
  }
  .empty-icon { font-size: 3.5rem; margin-bottom: 1.25rem; }
  .empty-state h2 {
    font-family: var(--font-head); font-weight: 800; font-size: 1.8rem;
    color: white; margin-bottom: 0.75rem;
  }
  .empty-state p { color: var(--mute); margin-bottom: 1.5rem; }
  .btn-clear {
    display: inline-block; padding: 0.7rem 1.5rem;
    background: var(--amber); color: var(--black); border-radius: 7px;
    font-family: var(--font-head); font-weight: 700; font-size: 1rem;
    text-decoration: none; transition: background 0.2s;
  }
  .btn-clear:hover { background: var(--amber-d); }

  @media (max-width: 640px) { .vehicle-grid { grid-template-columns: 1fr; } }
</style>
